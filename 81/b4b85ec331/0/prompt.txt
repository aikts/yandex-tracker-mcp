<task-notification>
<task-id>wmbmhpwwn</task-id>
<tool-use-id>toolu_01LrS4f6MtEgFE8pz1PzEMsM</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Workspace-Projects---ai-mcp-mcp-yandex-tracker/09712c88-417b-4525-86a4-5b24f348b787/tasks/wmbmhpwwn.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deep review of yandex-tracker-mcp PR #34 (issue_add_attachment): API compliance, security, architecture, quality, tests — adversarially verified" completed</summary>
<result>{"kept":[{"lens":"security","title":"Arbitrary server-side file read &amp; exfiltration via unvalidated file_path (remote/multi-tenant deployment)","claimed_severity":"critical","adjusted_severity":"high","verdict":"confirmed","location":"mcp_tracker/mcp/tools/issue_write.py:issue_add_attachment (added in PR #34); supported by mcp_tracker/tracker/custom/client.py:issue_add_attachment","explanation":"The tool reads `Path(file_path).read_bytes()` on a fully caller-controlled path with ZERO validation, then uploads the raw bytes as a Tracker attachment. The only gate is `check_issue_access(settings, issue_id)` (mcp_tracker/mcp/tools/_access.py:8), which merely compares the issue's queue PREFIX against TRACKER_LIMIT_QUEUES — it never inspects file_path. Because the server is a first-class remote/multi-tenant deployment (Settings.transport supports sse and streamable-http; oauth_enabled multi-tenant mode; mcp_server_public_url), a remote MCP client can call this tool over the network. Full exploit chain: attacker (or the LLM driving the client, e.g. via prompt injection embedded in issue/comment text that THIS server fetches with issue_get/issue_get_comments and feeds back to the model) invokes issue_add_attachment with file_path pointing at any file the server process can read, and issue_id pointing at an attacker-controlled queue/issue (in TRACKER_LIMIT_QUEUES if set, or any issue otherwise). The bytes are then sittable for download from that issue's attachments. High-value targets on a typical container/host: /proc/self/environ (leaks TRACKER_TOKEN, TRACKER_IAM_TOKEN, OAUTH_CLIENT_SECRET, OAUTH_ENCRYPTION_KEYS, REDIS_PASSWORD — i.e. the org-wide service credential and the key that decrypts every tenant's stored OAuth tokens in the Redis store), /etc/passwd, .env, ~/.ssh/id_* and known_hosts, application source, and in shared deployments other tenants' data on disk. Leaking OAUTH_ENCRYPTION_KEYS + the Redis store contents is a total compromise of all tenants' Yandex credentials. Confused-deputy dimension: in OAuth multi-tenant mode every other tool action is authorized per-user via get_yandex_auth(ctx), but file_path reads are performed with the SERVER's filesystem privileges, completely outside the per-user OAuth boundary — user A can read server/host secrets and other users' files that their Yandex identity never grants. Neither read-only mode nor queue limits mitigate file disclosure: read-only mode just unregisters the tool entirely (so it doesn't help when writes are intentionally enabled), and queue limits only restrict WHERE bytes land, not WHICH file is read. Path traversal/symlink following compounds it: no Path.resolve(), no allow-root, so `../` and symlinks pointing outside any intended directory are followed silently. Secondary DoS: read_bytes() loads the entire file into memory with no size pre-check (advertised up to 1024 MB) — a few concurrent calls against large files (or /dev/zero-like pseudo-files) can OOM the server; and since TrackerClient uses a non-configurable 10s TOTAL ClientTimeout, large uploads also fail unpredictably (functional bug) while still having consumed the memory.","recommendation":"Do not ship this as a network-reachable capability without hard gating. Concretely: (1) Make the tool opt-in via a dedicated setting (e.g. TRACKER_ATTACHMENTS_ENABLED, default false) AND refuse to register/execute it whenever settings.transport != 'stdio' or settings.oauth_enabled is true — server-side file paths are only meaningful for a local single-user stdio deployment. (2) Prefer eliminating server-side paths entirely: accept base64-encoded file CONTENT (plus filename) over the transport instead of a path, so no server filesystem access is implied and the data crosses the per-user auth boundary correctly. (3) If a path-based mode is kept, require an explicit allowed-root directory setting and enforce it after resolution: resolved = Path(file_path).resolve(strict=True); reject unless resolved.is_relative_to(allowed_root.resolve()); reject symlinks/non-regular files (resolve handles traversal). (4) Add a size pre-check via path.stat().st_size against a configurable max BEFORE read_bytes(), and ideally stream the upload rather than buffering the whole file in memory. (5) Document the threat model in README/manifest so operators enabling write mode remotely understand this exposes the filesystem.","verify_reasoning":"The PR diff and local code confirm every mechanical claim. issue_write.py issue_add_attachment does `path = Path(file_path); content = path.read_bytes()` on a fully caller-controlled `file_path` with the only guard being `check_issue_access(settings, issue_id)`. _access.py:8-12 confirms that helper only splits `issue_id` on \"-\" and compares the queue prefix to `tracker_limit_queues`; it never inspects `file_path`. There is no `Path.resolve()`, no allow-root, no symlink/regular-file check, and no `stat().st_size` pre-check before buffering the whole file via `read_bytes()` — so path traversal/symlink-following and unbounded in-memory reads are real. client.py builds FormData and POSTs the raw bytes, so the read file is exfiltrated as a downloadable attachment. settings.py confirms `transport: Literal[\"stdio\",\"sse\",\"streamable-http\"]` and `oauth_enabled`/`mcp_server_public_url`, so network-reachable and multi-tenant deployments are first-class, making the remote-attacker and confused-deputy framing valid: the Tracker call is authorized per-user via get_yandex_auth(ctx), but the filesystem read runs with server-process privileges outside that boundary. The DoS-via-large-read and the 10s total ClientTimeout interaction are also accurate.\n\nCaveats that argue against a flat \"critical\": the exposure is real but conditional on operator configuration — the tool only registers when `tracker_read_only=False` (write mode), and the most catastrophic targets cited (/proc/self/environ leaking OAUTH_ENCRYPTION_KEYS/Redis decryption keys) require BOTH the high-value oauth multi-tenant mode AND write mode enabled AND a network transport AND the attacker controlling a permitted issue/queue. In the common single-tenant static-token stdio deployment the blast radius is \"server can read any file it has FS perms for and stash it on an issue the caller can already reach\" — serious local-trust escalation but not org-wide tenant compromise. The arbitrary-file-read primitive itself is unambiguous and unvalidated, which is genuinely a vulnerability, but the writeup states the worst-case multi-tenant total-compromise scenario as if it were the default. Net: a legitimate, must-fix arbitrary file read / confused-deputy issue, best rated high with the critical ceiling reserved for the specific oauth+remote+write configuration. Recommendations (opt-in setting, base64 content instead of server paths, allow-root + resolve(strict=True) + is_relative_to, size pre-check/streaming) are all appropriate and supported by the code."},{"lens":"api","title":"Override filename sent via Content-Disposition only, not the documented ?filename= query param","claimed_severity":"medium","adjusted_severity":"low","verdict":"confirmed","location":"mcp_tracker/tracker/custom/client.py:issue_add_attachment (form.add_field(\"file\", content, filename=filename) + POST without params)","explanation":"The official Yandex Tracker API (POST /v3/issues/&lt;id&gt;/attachments/) documents the stored-name override as an OPTIONAL QUERY parameter `filename` (\"Additional parameters\" -&gt; filename = the new name the file is stored under). The PR instead passes the name only through the multipart part's Content-Disposition filename (aiohttp FormData.add_field(\"file\", content, filename=...)) and sends NO `params={\"filename\": ...}` on the request. This diverges from the documented contract. In practice the docs' curl example (--form 'file=@/path') implies Tracker derives the stored name from the part filename, so this likely works today, but relying on the part filename rat
... (truncated 51189 chars, full result in /private/tmp/claude-501/-Volumes-Workspace-Projects---ai-mcp-mcp-yandex-tracker/09712c88-417b-4525-86a4-5b24f348b787/tasks/wmbmhpwwn.output)</result>
<usage><agent_count>21</agent_count><subagent_tokens>652471</subagent_tokens><tool_uses>112</tool_uses><duration_ms>245279</duration_ms></usage>
</task-notification>