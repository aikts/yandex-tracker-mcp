/simplify

---

нет, нужно по всему PR

---

да, закоммить и запушь

---

да, сделай и это отдельным коммитом

---

смотри теперь мне нужно перекомпоновать изменения
должны отдельными комитами быть
- логические изменения в 5a82bbb106b55a169d186af7a642e2727d1faa3f
- в a0003c6615e2a3e3a9aa02d68411b4783e70cba7
- все что связано с добавлением template'ов issue и comments с сопуствующими необходимыми рефакторингами

порядок коммитов можешь сделать такой какой будет удобнее и легче
главное выделить вот эти три логических блока

---

пушь

---

[Image #1]
почему вы вдруг при обновлении позволили теперь отправлять не в виде объекта?

---

нам лишние изменения не надо делать
если что-то работало раньше - зачем это менять сейчас

---

[Image #2] ну ведь не полностью откачено оказалось - все еще есть возможность str передать

---

запушил?

---

проведи еще раз ревью, что точно нет хреновых реализаций никаких, все консистетно и убедись (вызывая реальное апи яндекс трекера .env с токеном в помощь) что вызывая по новому при создании задач ниче не ломается - можешь упражняться на очереди `AP`

---

да

---

запушил?

---

обнови описание PR если оно не совсем актуально

---

а почему для очереди AP вовращаются какие тоcomment templates
вроде же их нет в трекере

---

Найденные проблемы

1. Коллизия имён в fields — падение с сырым Python-исключением. Ключ в fields, совпадающий с именем выделенного параметра, ломает вызов:

issue_update(fields={"parent": null})
→ TrackerClient.issue_update() got multiple values for keyword argument 'parent'

Воспроизводится и на issue_create (fields={"assignee": ...}). Наружу течёт внутреннее имя модуля. Нужно либо мержить fields с выделенными параметрами, либо валидировать пересечение с понятной ошибкой.

2. Нет способа очистить assignee и parent. None в схеме означает «не передавать», assignee="" даёт 422, а обходной путь через fields заблокирован проблемой №1. Для deadline и storyPoints очистка работает (fields: {deadline: null}), для выделенных параметров — нет.

3. get_priorities не отдаёт id. Возвращаются version/key/name/order, при этом обе схемы (IssuePriorityRef.id) и докстринги ссылаются на этот инструмент как на источник id. Для сравнения, get_issue_types id отдаёт. Плюс список глобальный: minimal и important в AP недопустимы, узнаётся это только по 422 при создании.

4. IssueTypeRef / IssuePriorityRef / IssueParentRef пропускают пустой объект. type={} проходит клиентскую валидацию и приводит к невнятному 400 Incorrect data format от Трекера. У IssueComponentRef валидатор «ровно одно из id/name» есть — стоит добавить «хотя бы одно» и остальным.

5. Противоречивый объект резолвится молча. type={"id":"1","key":"task"} → выставился bug: побеждает id, key игнорируется без предупреждения. Поведение стоит либо задокументировать, либо запретить.

6. Стык шаблонов и issue_create неполный. Описание issue_create предписывает копировать значения fieldTemplates в параметры, но assignee шаблон отдаёт объектом пользователя, а параметр принимает только строку/число — прямое копирование падает на валидации. followers при этом объект принимает. Плюс макросы в шаблонах не разворачиваются: summary: "Релиз {{today}}" создал задачу с литеральным {{today}} (AP-141). Оба момента стоит проговорить в докстринге.

7. Мелочь в именовании. На вход storyPoints, на выход story_points; аналогично createdAt/created_at. При обратной подстановке ответа в апдейт это ловушка.

8. queue_get_fields для AP не вернул ни parent, ни estimation/originalEstimation, хотя parent через API выставляется, а originalEstimation виден в выдаче issues_find по AP-124. Список редактируемых полей неполный, а докстринг fields называет его обязательным источником истины перед созданием.


пробегись по этим проблемам, предложи решения какие то

---

давай все правим, я думаю можно поменять и добавить serialization_alias - желательно думаю везде приветси к одинаковым именам во избежание в будущем подобных проблем и зафиксировать в правилах проекта

---

да

---

Что стоит поправить

1. Асимметрия union-типов между create и update. issue_create принимает type="bug", priority=2, parent="AP-150", а issue_update на тех же значениях падает на pydantic-валидации, не доходя до API. Для агента это ловушка: он создаёт задачу скаляром, потом тем же способом пробует обновить и получает ошибку схемы. Либо добавить те же union'ы в update, либо убрать их из create.

2. queue_get_metadata с expand. Запросил components, versions, projects — эти ключи в ответе отсутствуют совсем, даже пустыми массивами. Непонятно, компонентов нет или expand не отработал. Пустые списки явно были бы полезнее.

3. get_priorities не находится через поиск инструментов. При отложенной загрузке я его так и не поднял — семантический поиск по «priorities», «critical/minor/blocker» возвращает что угодно, кроме него. У него, похоже, пустое или слишком короткое описание. При этом на него ссылаются docstring'и priority в обоих инструментах.

4. Шаблоны: два разных description. Верхнеуровневый — это описание самого шаблона, тело задачи лежит в fieldTemplates.description. У шаблона 14 они продублированы (причём верхний — с потерянной разметкой), у остальных верхний пустой. В docstring это не проговорено, легко взять не тот. Плюс у шаблона 11 ключа description нет вообще, у остальных "" — модель ответа не нормализует.

---

Что стоит поправить

1. Асимметрия union-типов между create и update. issue_create принимает type="bug", priority=2, parent="AP-150", а issue_update на тех же значениях падает на pydantic-валидации, не доходя до API. Для агента это ловушка: он создаёт задачу скаляром, потом тем же способом пробует обновить и получает ошибку схемы. Либо добавить те же union'ы в update, либо убрать их из create.

2. queue_get_metadata с expand. Запросил components, versions, projects — эти ключи в ответе отсутствуют совсем, даже пустыми массивами. Непонятно, компонентов нет или expand не отработал. Пустые списки явно были бы полезнее.

3. get_priorities не находится через поиск инструментов. При отложенной загрузке я его так и не поднял — семантический поиск по «priorities», «critical/minor/blocker» возвращает что угодно, кроме него. У него, похоже, пустое или слишком короткое описание. При этом на него ссылаются docstring'и priority в обоих инструментах.

4. Шаблоны: два разных description. Верхнеуровневый — это описание самого шаблона, тело задачи лежит в fieldTemplates.description. У шаблона 14 они продублированы (причём верхний — с потерянной разметкой), у остальных верхний пустой. В docstring это не проговорено, легко взять не тот. Плюс у шаблона 11 ключа description нет вообще, у остальных "" — модель ответа не нормализует.


по п1 - проверь сначала что update сможет нормально принимать не обернутые в объекты типы прежде чем делать такой же union как в create